/**
 * Variables de entorno para desarrollo local.
 * En producción se reemplaza con environment.prod.ts via Angular build.
 */
export const environment = {
  produccion: false,
  apiUrl: 'http://localhost:8080/api'
};
